<style>

  .btn-search {
    background-color: #fe424d;
    color: white;
    border-radius: 25px;
    padding: 0.45rem 0.9rem;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .btn-search:hover {
    background-color: #fe424d;
    color: white;
  }

  .btn-search i{
    display: inline;
    margin-right: 0.5rem;
  }

 .search-inp{
    border-radius: 25px;
    padding: 0.35rem 0.9rem;
    border: 1px solid #ccc;
    min-width: 220px;
  }

  /* center the search form visually in the navbar on larger screens */
  /* Make the navbar stick to the top when scrolling. We use sticky-top class on the nav
     element already but ensure visual stacking and avoid jumping by setting z-index. */
  .navbar{
    position: sticky; /* keep in flow but stick to top when scrolling */
    top: 0;
    z-index: 1030; /* above normal content, below modals (Bootstrap uses 1050+ for modals) */
    background: white;
  }
  /* Brand and navbar text styling */
  .navbar-brand {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1.15rem;
    font-weight: 700;
    color: #222222 !important;
    text-decoration: none;
  }
  .navbar-brand .brand-tagline {
    font-size: 0.75rem;
    font-weight: 500;
    color: #6c757d; /* muted */
    margin-left: 0.25rem;
    display: none; /* hidden on smaller screens */
  }
  @media (min-width: 768px){
    .navbar-brand .brand-tagline { display: inline-block; }
  }

  /* Primary nav links */
  .navbar-nav .nav-link {
    font-size: 0.95rem;
    font-weight: 700; /* make nav links bold */
    color: #222222 !important;
    padding: 0.5rem 0.6rem;
    transition: color 0.15s ease, background-color 0.15s ease;
  }
  .navbar-nav .nav-link:hover{
    color: #fe424d !important; /* brand color on hover */
    background-color: rgba(254,66,77,0.06);
    border-radius: 6px;
  }

  /* Right side links (Sign Up / Log In / Log out) - emphasize actions */
  .right-nav .nav-link b{
    font-weight: 700;
  }
  /* Ensure all right-side nav links are bold as well */
  .right-nav .nav-link{ font-weight: 700; }
  .right-nav .nav-link.signup {
    color: #fe424d !important;
  }
  .right-nav .nav-link.signup:hover{ background-color: rgba(254,66,77,0.06); }
  .nav-search-wrap{
    display: flex;
    justify-content: center;
    align-items: center;
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    width: 100%;
    max-width: 640px; /* visual max width */
    pointer-events: auto;
    padding: 0.25rem 0;
  }
  @media (max-width: 767px){
    /* On small screens keep search in normal flow inside collapse */
    .nav-search-wrap {
      position: static;
      transform: none;
      width: 100%;
      justify-content: center;
      padding: 0.5rem 0;
    }
    .search-inp{ min-width: 140px; }
  }

  /* keep collapse contents on a single line on larger screens */
  .navbar-collapse{
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: nowrap; /* avoid wrapping so items remain on one line */
  }
  @media (max-width: 767px){
    .navbar-collapse{ flex-wrap: wrap; } /* allow wrapping on small screens */
  }

  /* right side nav links (Airbnb your home / Sign Up / Log In / Log out) */
  .right-nav{
    display: flex;
    gap: 0.6rem;
    align-items: center;
  }
  .right-nav .nav-link{
    display: inline-flex;
    align-items: center;
    padding: 0.4rem 0.5rem;
  }
</style>



<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"><i class="fa-solid fa-compass"></i></a>
    <button class="navbar-toggler" 
    type="button" 
    data-bs-toggle="collapse" 
    data-bs-target="#navbarNavAltMarkup" 
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>

      <div class="nav-search-wrap">
        <form class="d-flex" role="search">
          <input class="form-control me-2 search-inp" type="search" placeholder="Search Destinations" aria-label="Search"/>
          <button class="btn btn-search" type="submit"><i class="fa-solid fa-magnifying-glass"></i>
            Search</button>
        </form>
      </div>
      <div class="navbar-nav ms-auto right-nav">
        <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if(!currentUser) { %>
        <a class="nav-link" href="/signup"><b>Sign Up</b></a>
        <a class="nav-link" href="/login"><b>Log In</b></a>
        <% } %> 
        <% if(currentUser) { %>
        <a class="nav-link" href="/logout"><b>Log out</b></a>
        <% } %>

      </div>
    </div>
  </div>
</nav>